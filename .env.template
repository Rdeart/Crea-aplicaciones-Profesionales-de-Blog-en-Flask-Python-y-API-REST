# Copia este archivo a ".env" y coloca tu clave privada de OpenAI.
# Nunca comitees el archivo .env con claves a git.

OPENAI_API_KEY=sk-REEMPLAZAME_POR_TU_CLAVE
# Opcional: instrucción completa para KAT IA si la quieres cargar desde servidor
# KAT_SYSTEM_INSTRUCTION="(pega aquí la instrucción completa si deseas que el servidor la use)"
